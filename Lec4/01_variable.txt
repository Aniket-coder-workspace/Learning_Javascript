1. Type of variables
    1. const --> value in this immutable
    2. let --> value (changeble) this is most probably usable
    3. var --> value (changeble) thid is not use nowdays

2. Table property -->
To print a values in (tabular) form in output 

